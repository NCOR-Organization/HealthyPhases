# INBOX — HealthyPhases

> Batched escalations from the fleet → human. ONLY true ambiguity or red-line items.
> Everything reversible should have been decided by the agent, not parked here.
> The human drains this on their own schedule; agents keep working meanwhile.
>
> Agent: when you add an entry, ALSO propose a default so the human can just reply "go".
> Running role agents? Include `[<agent>]` in the entry title so it's attributable.
> Human: answer inline under **Your call** — that's all; the fleet does the rest.
> Agent, on every run: for each entry with **Your call** filled in, act on the answer,
> graduate it into the MISSION Decisions Log, unblock the `[!]` backlog item, mark the
> entry `[x]`, and delete it once the decision is recorded.

Status keys: `[ ]` needs a human · `[x]` answered (moved to Decisions Log)

---

## [ ] <YYYY-MM-DD> — <short question title>
- **Context:** <what I was doing when I hit this>
- **The ambiguity:** <the actual fork in the road>
- **Options I see:** (a) ... (b) ...
- **What I tried / read:** <so it's clear this isn't laziness>
- **My recommended default:** <the option I'd take if no answer by <when>>
- **Your call:**  ⬅️ (human writes here)

---
