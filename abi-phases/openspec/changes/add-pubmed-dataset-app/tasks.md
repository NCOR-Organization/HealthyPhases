## 1. Local module and contracts
- [x] 1.1 Copy marketplace module, preserve provenance, update local references and package assets; verify with publisher/source tests and app checks.
- [x] 1.2 Define validated Protobuf commands/publication records and DatasetService adapter with stable keyed writes; verify with publisher/source tests and app checks.
## 2. Publisher
- [x] 2.1 Implement bounded query search and current PMC Cloud retrieval behind injected ports; verify with publisher/source tests and app checks.
- [x] 2.2 Implement durable requests, checkpointed publication, explicit retries and Dagster request/failure sensors; verify with publisher/source tests and app checks.
- [x] 2.3 Add Nexus search, preview, ingestion and status UI with module API registration; verify with publisher/source tests and app checks.
## 3. Consumer
- [x] 3.1 Add dataset discovery, ready-artifact previews and immutable manual source manifests in Phase v2; verify with publisher/source tests and app checks.
- [x] 3.2 Verify selected bytes and carry exact paper scope through every pipeline stage; verify with publisher/source tests and app checks.
- [x] 3.3 Add PubMed source selection and manual execution to the existing Phase v2 app; verify with publisher/source tests and app checks.
## 4. Review
- [x] 4.1 Test validation, acquisition success/failure, dataset publication, retries, orchestration, consumer isolation and UI behavior.
- [x] 4.2 Validate package/build/Make targets, document operations and deferred scheduling, and commit reviewable changes.
